{
  "name": "blackjack-trainer",
  "version": "1.0.0",
  "private": true,
  "description": "Blackjack trainer PWA - basic strategy, Hi-Lo counting practice, house odds. Simulated chips only.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check public/sw.js && node --check public/bj-audio.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
